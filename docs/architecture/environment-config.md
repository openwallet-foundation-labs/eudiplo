<!-- generated: 2025-08-30T13:29:55.548Z -->
# Configuration

This file gets auto-generated by the application. Do not edit it manually.

## config

| Key | Value | Notes |
| --- | ----- | ----- |
| `CONFIG_PRINT` | `false` | Enable config printing |
| `CONFIG_PRINT_FORMAT` | `text` | Format for printing config |
| `CONFIG_PRINT_ADVANCED` | `true` | Enable advanced config printing |
| `CONFIG_PRINT_FILE` | `../../docs/architecture/environment-config.md` | Output file path for the documentation |
| `CONFIG_IMPORT` | `false` | Run one-off config import on startup |
| `CONFIG_IMPORT_FORCE` | `false` | Force overwrite on config import |
| `CONFIG_FOLDER` | `../../assets/config` | Path to config import folder |

## auth

| Key | Value | Notes |
| --- | ----- | ----- |
| `OIDC` | `(unset)` | Enable OIDC mode |
| `OIDC_INTERNAL_ISSUER_URL` | `(unset)` | Internal issuer URL in OIDC mode [when OIDC is set → then required] |
| `OIDC_SUB` | `(unset)` | Claim to use as subject [when OIDC is set → then default="azp"] |
| `OIDC_ALGORITHM` | `(unset)` | Expected JWT alg [when OIDC is set → then default="RS256"] |
| `JWT_SECRET` | `your-secret-key-here-minimum-32-characters` | Local JWT secret (when OIDC is off) [when OIDC is set → otherwise default="supersecret"] |
| `JWT_ISSUER` | `eudiplo-service` | Local JWT issuer [when OIDC is set → otherwise default="eudiplo-service"] |
| `JWT_EXPIRES_IN` | `24h` | Local JWT expiration [when OIDC is set → otherwise default="24h"] |
| `AUTH_CLIENT_SECRET` | `root` | Client secret (local auth) [when OIDC is set → otherwise default="root"] |
| `AUTH_CLIENT_ID` | `root` | Client ID (local auth) [when OIDC is set → otherwise default="root"] |

## crypto

| Key | Value | Notes |
| --- | ----- | ----- |
| `CRYPTO_ALG` | `ES256` | The signing algorithm to use |

## database

| Key | Value | Notes |
| --- | ----- | ----- |
| `DB_TYPE` | `sqlite` | Database type |
| `DB_PORT` | `(unset)` | Database port [when DB_TYPE is {"override":true} \| "sqlite" → otherwise required] |
| `DB_USERNAME` | `(unset)` | Database username [when DB_TYPE is {"override":true} \| "sqlite" → otherwise required] |
| `DB_PASSWORD` | `(unset)` | Database password [when DB_TYPE is {"override":true} \| "sqlite" → otherwise required] |
| `DB_DATABASE` | `(unset)` | Database name [when DB_TYPE is {"override":true} \| "sqlite" → otherwise required] |

## general

| Key | Value | Notes |
| --- | ----- | ----- |
| `FOLDER` | `../../tmp` | Root working folder for temp files |
| `PUBLIC_URL` | `https://service.eudi-wallet.dev` | The public URL of the issuer |

## key

| Key | Value | Notes |
| --- | ----- | ----- |
| `KM_TYPE` | `db` | The key management type |
| `VAULT_URL` | `(unset)` | The URL of the Vault server [when KM_TYPE is {"override":true} \| "vault" → then required] |
| `VAULT_TOKEN` | `(unset)` | The token for accessing the Vault [when KM_TYPE is {"override":true} \| "vault" → then required] |

## log

| Key | Value | Notes |
| --- | ----- | ----- |
| `LOG_LEVEL` | `debug` | Application log level |
| `LOG_ENABLE_HTTP_LOGGER` | `false` | Enable HTTP request logging |
| `LOG_ENABLE_SESSION_LOGGER` | `false` | Enable session flow logging |
| `LOG_DEBUG_MODE` | `false` | Enable verbose debug logs |
| `LOG_FORMAT` | `pretty` | Log output format |

## registrar

| Key | Value | Notes |
| --- | ----- | ----- |
| `REGISTRAR_URL` | `(unset)` | The URL of the registrar |
| `REGISTRAR_OIDC_URL` | `(unset)` | The OIDC URL of the registrar [when REGISTRAR_URL is set → then required] |
| `REGISTRAR_OIDC_CLIENT_ID` | `(unset)` | The OIDC client ID of the registrar [when REGISTRAR_URL is set → then required] |
| `REGISTRAR_OIDC_CLIENT_SECRET` | `(unset)` | The OIDC client secret of the registrar [when REGISTRAR_URL is set → then required] |

## session

| Key | Value | Notes |
| --- | ----- | ----- |
| `SESSION_TIDY_UP_INTERVAL` | `3600` | Interval in seconds to run session tidy up |
| `SESSION_TTL` | `86400` | Time to live for sessions in seconds |

## storage

| Key | Value | Notes |
| --- | ----- | ----- |
| `STORAGE_DRIVER` | `local` | The storage driver to use |
| `LOCAL_STORAGE_DIR` | `../../tmp/uploads` | The directory to store files in when using local storage [when STORAGE_DRIVER is {"override":true} \| "local" → then default=undefined] |
| `S3_REGION` | `(unset)` | The AWS region for the S3 bucket [when STORAGE_DRIVER is {"override":true} \| "s3" → then required] |
| `S3_BUCKET` | `(unset)` | The name of the S3 bucket [when STORAGE_DRIVER is {"override":true} \| "s3" → then required] |
| `S3_ACCESS_KEY_ID` | `(unset)` | The access key ID for the S3 bucket [when STORAGE_DRIVER is {"override":true} \| "s3" → then required] |
| `S3_SECRET_ACCESS_KEY` | `(unset)` | The secret access key for the S3 bucket [when STORAGE_DRIVER is {"override":true} \| "s3" → then required] |
| `S3_ENDPOINT` | `(unset)` | The endpoint URL for the S3 service (for S3-compatible services) [when STORAGE_DRIVER is {"override":true} \| "s3"] |
| `S3_FORCE_PATH_STYLE` | `(unset)` | Whether to force path-style URLs for S3 [when STORAGE_DRIVER is {"override":true} \| "s3" → then default=false] |
| `S3_PUBLIC_BASE_URL` | `(unset)` | The public base URL for the S3 bucket [when STORAGE_DRIVER is {"override":true} \| "s3" → then required] |

## Other

| Key | Value | Notes |
| --- | ----- | ----- |
| `DB_HOST` | `(unset)` | [when DB_TYPE is {"override":true} \| "sqlite" → otherwise required] |
