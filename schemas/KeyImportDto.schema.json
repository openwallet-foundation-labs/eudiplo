{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/openwallet-foundation-labs/eudiplo/refs/heads/main/schemas/KeyImportDto.schema.json",
  "title": "KeyImportDto",
  "type": "object",
  "properties": {
    "kmsProvider": {
      "type": "string",
      "description": "KMS provider name to use for this key. Defaults to the configured default.",
      "example": "db"
    },
    "usageTypes": {
      "description": "Usage types for this key (access, signing, trustList, statusList).",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "access",
          "signing",
          "trustList",
          "statusList"
        ]
      },
      "example": [
        "signing"
      ]
    },
    "key": {
      "description": "The private key in JWK format.",
      "allOf": [
        {
          "$ref": "./Key.schema.json"
        }
      ]
    },
    "id": {
      "type": "string",
      "description": "Unique identifier for the key."
    },
    "description": {
      "type": "string",
      "description": "Description of the key."
    },
    "externalKeyId": {
      "type": "string",
      "description": "External key identifier for cloud KMS providers (e.g., AWS KMS Key ID, Azure Key Vault Key ID).\nThis field stores the provider-specific key reference, keeping it separate from the JWK.\nOnly populated for keys managed by external KMS providers (not for \"db\" provider)."
    }
  },
  "required": [
    "key",
    "id"
  ]
}