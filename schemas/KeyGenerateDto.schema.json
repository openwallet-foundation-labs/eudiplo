{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/openwallet-foundation-labs/eudiplo/refs/heads/main/schemas/KeyGenerateDto.schema.json",
  "title": "KeyGenerateDto",
  "type": "object",
  "properties": {
    "kmsProvider": {
      "type": "string",
      "description": "KMS provider to use (defaults to the configured default provider).",
      "example": "vault"
    },
    "description": {
      "type": "string",
      "description": "Optional human-readable description for the key."
    }
  }
}