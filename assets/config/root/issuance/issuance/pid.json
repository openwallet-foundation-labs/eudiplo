{
    "id": "pid",
    "description": "PID issuance with auth flow",
    "authenticationConfig": {
        "method": "auth",
        "config": {
            "url": "http://localhost:3000"
        }
    },
    "credentialConfigIds": [
        "pid"
    ]
}