/* import { zAuthorizationServerMetadata } from "@openid4vc/oauth2";
import { createZodDto } from "nestjs-zod";
import { z } from "zod";
 */
/* // export a portable type alias
export type Oauth2AuthorizationServerResponse = z.infer<
    typeof zAuthorizationServerMetadata
>;

// use a class for Nest runtime (different name to avoid name collision)
export class Oauth2AuthorizationServerResponseDto extends createZodDto(
    zAuthorizationServerMetadata,
) {} */
